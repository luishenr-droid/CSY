services:
  - type: web
    name: nzn-simulador-carga
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: TARGET_URL
        sync: false
      - key: SIMULATOR_PASSWORD
        sync: false
